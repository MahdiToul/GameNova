const search=document.querySelector('#search');const cards=[...document.querySelectorAll('.card')];const filters=[...document.querySelectorAll('.filter')];const empty=document.querySelector('#empty');let category='all';function render(){const q=search.value.trim().toLowerCase();let visible=0;cards.forEach(card=>{const okCategory=category==='all'||card.dataset.category===category;const okSearch=card.dataset.name.toLowerCase().includes(q);const show=okCategory&&okSearch;card.style.display=show?'block':'none';if(show)visible++});empty.style.display=visible?'none':'block'}search.addEventListener('input',render);filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');category=btn.dataset.category;render()}));document.querySelectorAll('.download').forEach(btn=>btn.addEventListener('click',()=>alert('صفحه دانلود این بازی در نسخه بعدی اضافه می‌شود.')));