const search=document.querySelector('#search');const cards=[...document.querySelectorAll('article')];const filters=[...document.querySelectorAll('.filter')];const empty=document.querySelector('#empty');let category='all';function render(){const q=(search?.value||'').trim().toLowerCase();let visible=0;cards.forEach(card=>{const name=card.querySelector('h3').textContent.toLowerCase();const okCategory=category==='all'||card.dataset.category===category;const show=okCategory&&name.includes(q);card.style.display=show?'block':'none';if(show)visible++});if(empty)empty.style.display=visible?'none':'block'}search?.addEventListener('input',render);filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');category=btn.dataset.category;render()}));const info={SuperTuxKart:{icon:'🏎️',cat:'مسابقه‌ای',text:'بازی مسابقه‌ای رایگان و متن‌باز.'},Luanti:{icon:'🌲',cat:'ماجراجویی',text:'دنیایی بلوکی برای ساخت‌وساز و اکتشاف.'},OpenTTD:{icon:'🚂',cat:'استراتژی',text:'بازی مدیریت و ساخت شبکه حمل‌ونقل.'}};const modal=document.querySelector('#modal');const close=document.querySelector('#close');const title=document.querySelector('#modalTitle');const text=document.querySelector('#modalText');const icon=document.querySelector('#modalIcon');const cat=document.querySelector('#modalCategory');document.querySelectorAll('.details').forEach(btn=>btn.addEventListener('click',()=>{const data=info[btn.dataset.game];icon.textContent=data.icon;cat.textContent=data.cat;title.textContent=btn.dataset.game;text.textContent=data.text+' در نسخه بعدی لینک دریافت قانونی این بازی نیز اضافه می‌شود.';modal.classList.add('show');modal.setAttribute('aria-hidden','false')}));close?.addEventListener('click',()=>{modal.classList.remove('show');modal.setAttribute('aria-hidden','true')});modal?.addEventListener('click',e=>{if(e.target===modal)close.click()});render();