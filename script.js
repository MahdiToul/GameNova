const search=document.querySelector('#search');const cards=[...document.querySelectorAll('article')];const filters=[...document.querySelectorAll('.filter')];const empty=document.querySelector('#empty');let category='all';function render(){const q=(search?.value||'').trim().toLowerCase();let visible=0;cards.forEach(card=>{const name=card.querySelector('h3')?.textContent.toLowerCase()||'';const type=card.querySelector('small')?.textContent.toLowerCase()||'';const okCategory=category==='all'||card.dataset.category===category;const show=okCategory&&(name.includes(q)||type.includes(q));card.style.display=show?'block':'none';if(show)visible++});if(empty)empty.style.display=visible?'none':'block'}search?.addEventListener('input',render);filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');category=btn.dataset.category;render()}));const modal=document.querySelector('#modal');const close=document.querySelector('#close');const title=document.querySelector('#modalTitle');const text=document.querySelector('#modalText');document.querySelectorAll('.details').forEach(btn=>btn.addEventListener('click',()=>{title.textContent=btn.dataset.title||btn.closest('article')?.querySelector('h3')?.textContent||'جزئیات';text.textContent=btn.dataset.text||'اطلاعات این بخش به‌زودی تکمیل می‌شود.';modal.classList.add('show');modal.setAttribute('aria-hidden','false') }));close?.addEventListener('click',()=>{modal.classList.remove('show');modal.setAttribute('aria-hidden','true')});modal?.addEventListener('click',e=>{if(e.target===modal)close.click()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal?.classList.contains('show'))close.click()});render();