document.addEventListener('DOMContentLoaded',()=>{const search=document.querySelector('#search'),cards=[...document.querySelectorAll('article')],filters=[...document.querySelectorAll('.filter')],empty=document.querySelector('#empty'),modal=document.querySelector('#modal'),close=document.querySelector('#close'),title=document.querySelector('#modalTitle'),text=document.querySelector('#modalText');let category='all';function render(){const q=(search?.value||'').trim().toLowerCase();let visible=0;cards.forEach(card=>{const name=card.querySelector('h3')?.textContent.toLowerCase()||'',type=card.querySelector('small')?.textContent.toLowerCase()||'';const show=(category==='all'||card.dataset.category===category)&&(!q||name.includes(q)||type.includes(q));card.style.display=show?'block':'none';if(show)visible++});if(empty)empty.style.display=visible?'none':'block'}function openModal(btn){if(!modal)return;const card=btn.closest('article');title.textContent=btn.dataset.title||card?.querySelector('h3')?.textContent||'جزئیات';text.textContent=btn.dataset.text||card?.querySelector('p')?.textContent||'اطلاعاتی برای این بخش وجود ندارد.';modal.classList.add('show');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'}function closeModal(){if(!modal)return;modal.classList.remove('show');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''}search?.addEventListener('input',render);filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));btn.classList.add('active');category=btn.dataset.category;render()}));document.querySelectorAll('.details').forEach(btn=>btn.addEventListener('click',e=>{e.preventDefault();openModal(btn)}));close?.addEventListener('click',closeModal);modal?.addEventListener('click',e=>{if(e.target===modal)closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});render()});